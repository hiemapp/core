export interface DriverInputEvent {
    input: {
        name: string,
        value: string
    }
}

export interface UpdateEvent {}

export interface ConnectionOpenEvent {}

export interface ConnectionCloseEvent {}